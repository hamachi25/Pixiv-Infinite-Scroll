name: workflow dispatch
on:
    push:
        branches:
            - workflow
    workflow_dispatch: {}

jobs:
    hello:
        runs-on: ubuntu-latest
        steps:
            - name: hello
              run: "echo Hello"
